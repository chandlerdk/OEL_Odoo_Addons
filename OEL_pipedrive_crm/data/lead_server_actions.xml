<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Server action visible in CRM Leads: Actions → Convert to Prospect -->
    <record id="action_lead_convert_to_prospect" model="ir.actions.server">
        <field name="name">Convert to Prospect</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="binding_model_id" ref="crm.model_crm_lead"/>
        <field name="state">code</field>
        <field name="code">
action = records.action_convert_to_prospect()
        </field>
        <field name="binding_type">action</field>
    </record>
</odoo>
